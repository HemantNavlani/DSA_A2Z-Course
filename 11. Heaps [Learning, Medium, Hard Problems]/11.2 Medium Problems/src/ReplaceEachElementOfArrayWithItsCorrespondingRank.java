import java.util.HashMap;
import java.util.Map;

public class ReplaceEachElementOfArrayWithItsCorrespondingRank {
    //import java.util.*;
    //public class Solution {
    //    public static List<Integer> replaceWithRank(List<Integer> arr, int n) {
    //        List<Integer> list = new ArrayList<>();
    //        for (int i = 0;i<arr.size();i++){
    //            list.add(arr.get(i));
    //        }
    //        Collections.sort(list);
    //        HashMap<Integer,Integer> mp = new HashMap<>();
    //        int temp = 1;
    //        for (int i = 0;i<list.size();i++){
    //            if (!mp.containsKey(list.get(i))) mp.put(list.get(i),temp++);
    //        }
    //        for (int i = 0;i<arr.size();i++){
    //            arr.set(i,mp.get(arr.get(i)));
    //        }
    //        return arr;
    //    }
    //}
}